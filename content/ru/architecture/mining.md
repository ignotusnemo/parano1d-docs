# Архитектура майнинга

**Одной вычислительной мощности недостаточно для создания блоков. Майнинг
работает с состоянием и допускается только после построения доказательства.**
Производитель блока обязан хранить текущее состояние и установить точный
следующий переход до начала поиска nonce. Поэтому независимый майнер — это
доказывающая полная нода с подключённой вычислительной мощностью, а не
stateless-устройство перебора хешей.

Майнинг состоит из двух последовательных фаз:

1. выбрать транзакции, построить переход состояния и доказать полный блок без
   nonce;
2. найти 128-битный nonce фиксированного заголовка Poseidon2b.

Такой порядок не позволяет тратить proof of work на блок, переход которого ещё
не доказан.

## Построение шаблона

Нода начинает с канонического tip и текущего мемпула. Она выбирает
неконфликтующие логические транзакции по ставке комиссии, соблюдая ограничения
состояния, сегментов и класса доказательства. Затем нода:

- вычисляет coinbase и запланированную системную выплату, если она требуется;
- назначает новые идентификаторы создания выходов;
- получает канонические записи слотов;
- вычисляет корни транзакций и post-state;
- строит публичный вход `HistoryStep`;
- доказывает новый терминал.

После этого всё, кроме nonce заголовка, неизменно.

## B64 и B255

Стек доказательств поставляется с двумя аутентифицированными классами матриц:

| Класс | Размерность гиперкуба | Эффективная вместимость страниц |
|---|---:|---:|
| B64 | `m=23` | До 64 |
| B255 | `m=24` | До 255 |

Каждый майнер начинает с B64. Нода может перейти на B255, когда полный замер
подготовки доказательства показывает, что большее отношение подходит для
15-секундной цели блока. Решение основано на end-to-end времени подготовки, а
не на одной внутренней фазе proving.

Оба класса доказывают одно отношение консенсуса. Это варианты вместимости, а не
разные правила валидности блока.

## Планирование CPU

Встроенный майнер использует единый пул CPU и для построения доказательства, и
для поиска nonce. Два независимых задания, занимающих все ядра, одновременно
не запускаются.

Локальная работа кошелька имеет приоритет. Если пользователь отправляет
транзакцию или запускает консолидацию во время майнинга, нода на локальной
границе приостанавливает либо уступает CPU-работу майнера, завершает
авторизацию и отправку транзакции, после чего продолжает майнинг. Сетевые
правила, приоритет пиров и глобальная политика комиссий не меняются.

## Обновление шаблона

Шаблон перестраивается по событиям, меняющим полезную работу:

- новый канонический tip;
- смена адреса выплаты майнера;
- первая транзакция, поступившая в шаблон только с coinbase;
- инвалидирование выбранных транзакций.

Heartbeat в 75 секунд служит страховкой, а не основным циклом обновления. Уже
доказанный шаблон остаётся привязан к исходной выплате и набору транзакций; после
построения доказательства он не изменяется.

## Встроенный майнер

Встроенный майнер работает внутри процесса ноды. Нода отвечает за выбор
транзакций, построение доказательства, PoW и отправку блока. Число CPU-потоков
и адрес выплаты настраиваются через Core или GUI.

Майнинг начинается только после синхронизации и подключения не менее двух
аутентифицированных пиров. Локально найденный блок запечатывается, фиксируется и
объявляется тем же путём приёма, что и блок от пира.

## Внешний майнер

Внешний майнер выносит из ноды только поиск nonce. Он запрашивает непрозрачный
одноразовый шаблон, содержащий:

- идентификатор шаблона;
- точное 16-полевое расписание PoW Poseidon2b;
- target;
- срок действия и отображаемые метаданные.

В ответ возвращается один 128-битный little-endian nonce. Нода проверяет его
относительно всё ещё действующего шаблона, запечатывает уже доказанный блок и
инвалидирует шаблон. Внешний шаблон истекает через 30 секунд и не может быть
повторно использован после смены tip или успешной отправки.

Внешний worker не получает права заменять транзакции, менять состояние,
комиссии или coinbase. Пользовательский coinbase требует одновременно явной
настройки ноды и bearer key.

## Выбор ветви

Майнинг продолжает валидную цепочку с наибольшей накопленной работой. При равной
работе детерминированно выбирается лексикографически меньший хеш блока.
Кандидаты, заменяющие жёстко финализированный префикс, не рассматриваются.

Развёртывание описано в разделах [«Встроенный майнер»](../operate/internal-mining.md)
и [«Внешний майнер»](../operate/external-miner.md). Точное отношение nonce
приведено в разделе [«Proof of work»](../protocol/proof-of-work.md).
